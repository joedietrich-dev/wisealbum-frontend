import styled from "styled-components/macro";

const WordMark = styled.p`
  font-size: 2rem;
  margin: 0;
  margin-right: auto;
  font-weight: bold;
  justify-self: start;
`;

export default WordMark;
